<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.finalProject.mappers.adminInquiryMapper">
	
	<!-- 문의 총 개수 가져오기 -->
	<select id="selectInquiryCnt" resultType="int">
		select count(*) from
		inquiries;
	</select>

	<!-- 관리자 뷰 문의 리스트 가져오기(with pagingInfo) -->
	<select id="selectAllInquiryWithPi" resultType="InquiryDetailDTO">
		select
		inquiry_no, member_id, product_no, inquiry_title, inquiry_content, inquiry_reg_date, inquiry_status, inquiry_type
		from inquiries
		order by inquiry_no desc
		limit #{startRowIndex}, #{viewDataCntPerPage}
	</select>
	

</mapper>