<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.finalProject.mappers.levelMapper">

	<!-- 등급 정보 가져오기 -->
	<select id="selectLevelInfos" resultType="LevelDTO">
		select * from levels;
	</select>


</mapper>