<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.finalProject.mappers.noticeMapper">

<!-- 공지사항 목록 조회, 내림차순 -->
	<select id="getAllNotices" resultType="NoticeDTO">
		select * from notices order by notice_no asc
    </select>
    
<!--     공지사항 작성 -->
	<insert id="saveNotices">
		insert into notices (notice_title, notice_content, reg_date, admin_id, notice_type)
		values (#{noticeTitle}, #{noticeContent}, #{regDate}, #{adminId}, #{noticeType})
	</insert>
    
</mapper>
  