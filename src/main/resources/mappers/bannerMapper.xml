<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.finalProject.mappers.bannerMapper.">

<!--     배너 삽입
    <insert id="insertBanner" parameterType="BannerVO">
        INSERT INTO banners (file_name, file_path, description)
        VALUES (#{fileName}, #{filePath}, #{description})
    </insert>

    배너 목록 조회
    <select id="getBanners" resultType="BannerVO">
        SELECT * FROM banners ORDER BY file_name
    </select>

    선택된 배너 업데이트
    <update id="updateSelectedBanner" parameterType="String">
        UPDATE banners SET selected = 1 WHERE file_name = #{fileName}
    </update> -->

</mapper>
