<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.finalProject.mappers.memberMapper">
	<!-- 쿼리문 -->

	<!-- 로그인 -->
	<select id="login" resultType="LoginDTO">
		select * from members where
		member_id = #{member_id} and member_pwd = #{member_pwd}
	</select>

	<!-- id 중복 체크 -->
	<select id="idDuplicate" resultType="int">
		select count(*) from members where member_id = #{value}
	</select>
	
	<!-- email 중복 체크 -->
	<select id="emailDuplicate" resultType="int">
		select count(*) from members where email = #{value}
	</select>
	
	<!-- phone_number 중복 체크 -->
	<select id="phoneDuplicate" resultType="int">
		select count(*) from members where phone_number = #{value}
	</select>

</mapper>