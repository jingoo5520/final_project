<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.finalProject.mappers.memberMapper">
   <!-- 쿼리문 -->
   
<!--    아이디 중복검사  -->
   <select id="selectDuplicateId" resultType="int">
	   select count(*) from members where member_id = #{tmpUserId}
   </select>
   
<!-- 	회원삽입 -->
	<insert id="insertMember">
		insert into members (member_id, member_pwd, member_name, phone_number, email) values(#{member_id}, sha1(md5(#{member_pwd})), #{member_name}, #{phone_number}, #{email})
	</insert>
   
</mapper>